def extract_keyframes_from_path(video_path):
    print(f"📸 Extracting keyframes from: {video_path}")
    # TODO: Replace this mock with actual MediaPipe or OpenCV keyframe logic
    return [
        { "rep": 1, "duration_sec": 2.5, "estimated_RPE": 8.5, "estimated_RIR": "1–2" },
        { "rep": 2, "duration_sec": 2.6, "estimated_RPE": 9.0, "estimated_RIR": "0–1" }
    ]
